from typing import Literal

PetType = Literal["cat", "dog", "bird"]

class Pet:
    __allowed = {"cat", "dog", "bird"}

    def __init__(self, kind: PetType):
        if kind not in self.__allowed:
            raise ValueError(f"Invalid kind: {kind}")
        self.kind = kind