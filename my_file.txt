
Use Fullstack Cleanup agent.

Clean the entire module: 

Scope:
- Python (Django)
- JavaScript layout managers
- HTML templates

Rules:
- Do NOT change behavior
- Do NOT break API or UI
- Do NOT rename functions, classes, ids, or CSS classes
- Do NOT remove anything used across files

Tasks:
1. Remove unused imports in Python and JS
2. Remove duplicate imports
3. Remove clearly unused local variables
4. Remove dead commented-out code
5. Clean redundant HTML markup only if safe

Very important:
- Check cross-file usage before removing anything
- Keep all DOM ids and classes used by JavaScript
- Keep all event listeners intact
- Keep Django implicit imports (admin, signals, serializers)

Output:
- Apply only high-confidence changes
- Group changes by file
- Explain what was removed and why
- Flag anything suspicious instead of deleting it